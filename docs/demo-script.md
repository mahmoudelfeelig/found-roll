# Found Roll demo script

Target length: **3:48**. Hard stop: **3:55**. The public video must remain under the event's four-minute limit.

Record the entire video—from the opening disclosure through the final passport—as one continuous, unedited screen capture. Keep window and tab changes visible, including the move from the app to Google Cloud Console and the architecture diagram. Do not pause, cut, splice, hide a retry, substitute fixture output, or edit a failed run into a successful one.

This is the recording contract, not a substitute for a video receipt. Entrant/team eligibility and the research-informed story mode are confirmed. Publish only the release tagged `v1.0.0`, and only after the private release verifier proves the active Google Cloud Free Trial boundary, required APIs/IAM/quota, five live Gemini/ADK workflows, log privacy, anonymous judge access, and a public YouTube/Vimeo runtime below four minutes. No public proof-of-action receipt is implemented or part of this recording.

## Recording gate

Do not record the final demo until the following are true for the same frozen commit and fixture version:

- `found-roll-app` and `found-roll-simulator` are deployed as separately addressable Cloud Run services.
- The app health response reports the live analyst, pinned model, Firestore repository, Cloud Tasks, and HTTP simulator modes—not fixture or inline modes.
- `scripts/prepare-canonical-run.ps1` has reset both services from an authenticated terminal or Cloud Shell. Its identifier-only receipt binds the canonical reset case, workflow epoch, staff-only original, model-authorized preview, current active pair, and live-mode health; `inventory_legacy_health_compatibility` is `false`. The browser has no reset authority; **Refresh case** is not reset. The recording uses that deliberately unarmed prepared case and explicitly starts its bounded analysis; it does not create a fresh ordinary intake.
- The independent demo, staff, and supervisor values have been loaded into their matching in-memory fields without exposing them. The strict non-mutating all-three probe has succeeded, and its configured actor IDs are recorded as identifiers only.
- The browser is a clean session at the hosted project URL, with judge-appropriate access already verified.
- The Cloud Console tabs are pre-opened to the exact app revision, simulator revision, Firestore passport, Cloud Task/queue, and correlated model/trace view for the same prepared case. Each tab has the shared case/task/model correlation ready to read without scrolling or searching on camera.
- The narration describes documented operational friction and never implies that a team member personally experienced the synthetic route.
- All screens, simulator responses, and narration say `SIMULATED` whenever Relay Post or the fictional custodians are shown.
- A log scan from the rehearsal contains no private answer, raw token, signed URL, model prompt/response, or claimant text.

## Exact run of show

| Time | Screen action | Narration | Evidence that must remain visible |
| --- | --- | --- | --- |
| 0:00–0:06 | Start on the hosted root Judge Walkthrough. | “This public, read-only walkthrough exposes a redacted closed synthetic case without credentials, media access, or mutation authority. The protected flow follows.” | `PUBLIC · READ-ONLY`; fixed synthetic case; no password fields; explicit internal-record limitation. |
| 0:06–0:18 | Open the already authenticated staff workspace with the three fictional custodian names, prepared case ID, connected live-service label, and current server-derived preview visible. | “Found Roll uses fictional operators, synthetic item data, and a clearly simulated relay. Gemini, Google ADK, and the Google Cloud workflow shown here are live. Public recovery guidance documents the handoff gap; it is research-informed, not a claim that this synthetic route happened to us.” | Found Roll title; Grand Hall, Metro Loop, and Northport Air; prepared case ID; live connection label. Do not expose credential fields. |
| 0:18–0:32 | Open the identifier-only canonical preparation receipt from the authenticated terminal/Cloud Shell, then return to the staff workspace. | “Reset and evidence preparation are admin-only terminal operations. This receipt binds this filmed case and workflow epoch to the staff-only original and model-authorized preview; the browser cannot reset it. I will now explicitly start bounded analysis on this unarmed prepared case.” | Canonical reset timestamp, same case ID and workflow epoch, evidence record IDs/digests, `active_for_analysis=true`, production modes, and inventory legacy compatibility `false`; no secret or raw media. |
| 0:32–0:43 | Click **Import Intake**, select **Sensitive or regulated**, show one Northport category-specific safety stop, then click **Acknowledge safety stop** to cancel and close that branch. Do not submit it. | “Before upload, sensitive categories stop locally and route to the selected custodian’s specialist procedure. This branch creates no case and calls no model.” | Tenant/category action and retention copy; no upload control. |
| 0:43–1:05 | Return to the prepared case and click **Start Bounded Analysis**. Wait for `ANALYZING`, then `CLARIFICATION_REQUIRED`. | “The custody engine already fixed the eligible candidate ordering and permitted private attribute. The Case Analyst can inspect only that authorized packet and propose a source-linked non-leading question. It cannot choose claim sufficiency, accept a claim, or change custody.” | The same prepared case ID/workflow epoch; `ANALYZING`, then `CLARIFICATION_REQUIRED`; state/version change; bounded-analysis status. Task and model identifiers are shown later in the correlated Cloud proof. |
| 1:05–1:24 | Click **Issue & Open Separate Claimant Link**. In the newly opened claimant tab, submit the requested private suffix without showing it to the camera. | “This one-time link is bound to the case version and expiry. Its fragment is scrubbed into tab memory, and the claimant sees a purpose-built proof projection—not the staff passport, candidate library, or expected answer.” | Separate claimant tab; one question; coarse case copy; visible expiry; scrubbed address bar; successful accepted-evidence message. The fixture image is claimant context, not a candidate record. |
| 1:24–1:37 | Switch back to the existing staff tab. Let **Refresh case** reload if needed, then click **Record Identity Attestation**. | “Deterministic code accepted the private evidence, but release is still blocked. The staff action records the configured server-derived actor and an allowed method; it stores no ID image.” | `CLAIM_EVIDENCE_ACCEPTED` before the click and `IDENTITY_ATTESTED` afterward; no identity document or method dialog is claimed. |
| 1:37–1:49 | Click **Approve Valuable Item**, then **Reserve SIMULATED Relay** when it becomes available. | “Approval uses the distinct supervisor boundary, whose configured actor is derived by the server. The model cannot approve or reserve this item.” | Approval/reservation state changes and the **Open Relay Terminal** action. No reason-entry dialog is claimed. |
| 1:49–2:11 | Open the relay terminal, present the custodian credential, then the claimant credential. | “These short-lived credentials attest one-time presentation to a permanently labeled simulator. They do not prove ownership, identity, possession, or a physical handoff.” | Permanent `SIMULATED RELAY` banner; reservation ID/eTag/expiry; both credential cards change to used; `CLAIMANT_PRESENT`. |
| 2:11–2:25 | Click **Send simulated handoff callback**. After the passport closes, click **Queue duplicate task delivery** once. | “The staff-confirmed release is processed through the signed simulated service boundary. I am now queueing a second delivery of the completed release command; it is acknowledged without another custody event.” | `CLOSED`; then “DUPLICATE TASK DELIVERY ACKNOWLEDGED · NO NEW EVENT.” Do not call this control a callback replay. |
| 2:25–2:39 | Click **View closed passport**, then **View Custody Evidence**. Scroll the Item Passport event dialog briefly, then click **Export JSON manifest** and open the downloaded file. | “The dialog shows the application’s hash-linked service events and internal-consistency summary. The downloaded closed-service manifest preserves that record for inspection, but it is not independent proof of a physical transfer.” | Visible event count, case version, event actors/IDs, and the explicit internal-record limitation. The JSON is a real download; final hash, evidence digests, and manifest booleans are visible only after opening that file, not directly in the dialog. |
| 2:39–2:51 | Switch to the Cloud Run service pages. | “This is the serving Found Roll app revision and the separately deployed simulator revision for the filmed run.” | Project, region, both service names, serving revisions, and timestamps. Keep the app revision legible; do not spend this interval on unrelated console navigation. |
| 2:51–3:04 | Open the Firestore passport for the filmed case. | “The same case is now closed in durable state. Its identifier matches the prepared case and the passport I just showed.” | The shared case ID, workflow epoch, terminal state, and revision/correlation fields that are safe to show. |
| 3:04–3:17 | Open the Cloud Task/queue evidence for that case. | “This task is the bounded workflow delivery for the same case. Its OIDC-backed receipt is payload-free, and the duplicate delivery did not add an event.” | The same case correlation, task name/status, delivery/replay outcome, and timestamp. Never reveal a task body. |
| 3:17–3:31 | Open the correlated Vertex/ADK evidence and redacted log/trace view. | “This is the pinned model and bounded ADK trajectory for that same case/task correlation. The agent proposed within the deterministic packet; policy—not the model—accepted evidence and released nothing on its own.” | Same case/task correlation, configured model, run/invocation evidence, bounded protocol outcome, and timestamp. Do not expose prompts, tool arguments/results, media, or account email. |
| 3:31–3:40 | Keep the correlated Vertex/ADK evidence and redacted log/trace view open long enough to read the matching case/task/model proof. | “This view completes the single-run correlation. The private release evidence, not a public receipt, records the identifiers and the five-run gate.” | Same case/task correlation, configured model, run/invocation evidence, bounded protocol outcome, and timestamp. Do not expose prompts, tool arguments/results, media, or account email. |
| 3:40–3:48 | Open the checked architecture image at the authority boundary, then return to the closed staff workspace. | “The agent proposes; deterministic policy authorizes every transition. Found Roll coordinates a policy-bound return across fictional custodians and a simulated relay.” | Readable agent/policy boundary, permanent `SIMULATED` disclosure, product name, and closed state. |

## Presenter language guardrails

Say:

- “accepted claim evidence,” not “ownership verified”;
- “the custody engine fixed the candidate packet and allowed attribute; the analyst proposed a question,” not “the agent chose the candidate” or “the agent decided the evidence was sufficient”;
- “staff identity attestation,” not “identity was proven by AI”;
- “simulated relay service attestation,” not “courier confirmed possession”;
- “application-enforced, hash-linked event manifest,” not “immutable ledger”;
- “live model and Google Cloud services in this recorded run,” only after the console receipts are visible;
- “measured on 16 synthetic fixtures,” only if the final evaluation report contains that exact completed result.
- “descriptive local candidate top-three proxy 2/2 and question-bearing usefulness proxy 3/3,” only with “insufficient sample, thresholds incomplete, zero Gemini and zero Google Cloud calls” in the same statement.

Never say:

- that Grand Hall, Metro Loop, Northport Air, or Relay Post is a real integration;
- that a QR code or token proves a person physically possessed an item;
- that the analyst selected a candidate, determined claim sufficiency, or accepted a claim;
- that the system is human-free;
- that the system is production-ready, compliant, or accurate in the real world;
- that a local fixture response is a live Gemini, ADK, Cloud Tasks, or remote simulator result.

## Camera and edit notes

- Capture at 1440 × 900 or higher so the dense desktop interface remains legible.
- Use one continuous take. Keep every app, console, and architecture transition visible; do not pause or conceal a failed segment.
- Keep browser zoom at 100% and the system cursor visible.
- For the checked architecture PNG, keep browser zoom at 100%, use the raw image viewer's native-pixel toggle, and pan through the three recorded regions rather than shrinking all 3568 pixels into one frame.
- Use restrained callouts only for model run ID, state changes, replay result, and Cloud revision evidence.
- Avoid kinetic captions, synthetic narration, fake terminal text, background music that obscures speech, or a generic AI montage.
- Keep the staff status bar, case/version inspector, and relay case/version console visible when those surfaces are on screen. Use the external receipt to bind task/model identifiers that the product UI does not display.
- Redact the Google account email and billing details in Cloud Console, but do not crop away the project, service, revision, time, or matching case/task/trace identifiers.
- Add accurate English subtitles and verify the published YouTube or Vimeo runtime before pasting the URL into Devpost.

## Private recording receipt and future proof-of-action release check

Immediately after recording, save a private text receipt alongside the frozen release. Resolve every value from the named authoritative source instead of transcribing it from memory:

| Field | Authoritative source |
| --- | --- |
| Submitted commit/tag | Public `v1.0.0` Git tag and the immutable commit it resolves to |
| Repository/tag and judge access | Anonymous checkout of `https://github.com/mahmoudelfeelig/found-roll` at `v1.0.0` |
| Fixture version | Frozen release record; expected release fixture is `camera-pouch-v1` |
| App Cloud Run revision | Final canonical-run receipt and Cloud Run revision page shown in the take |
| Simulator Cloud Run revision | Final canonical-run receipt and Cloud Run revision page shown in the take |
| Case ID | The filmed canonical-run receipt |
| Model name and run ID | The filmed canonical-run receipt |
| Cloud Task name | The filmed canonical-run receipt |
| Trace/correlation ID | The filmed run's redacted Cloud Logging view and receipt |
| Final event hash | The filmed canonical-run and chain-audit receipts |
| Evaluation report digest | SHA-256 of `evaluation/results.json` at `v1.0.0` |
| Public video URL and duration | Public YouTube/Vimeo player plus its observed runtime; the runtime must be below four minutes |

Do not put credentials, private answers, signed claimant links, or service secrets in this receipt.

No public proof-of-action receipt is implemented or filmed. If a future public artifact is proposed, stop and obtain explicit authorization plus a separate privacy review before creating, exposing, or describing it. It cannot replace the private five-run evidence or prove model quality, claim sufficiency, ownership, identity, possession, physical handoff, or independent immutability.
